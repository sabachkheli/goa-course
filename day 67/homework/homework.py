

return sum(range(min(a, b), max(a, b) + 1))

return 0 if n < 2 else (n - 1) * 2



   for num in seq:
        if seq.count(num) == 1:
            x = num
        elif seq.count(num) == 2:
            y = num
    return x * x * y